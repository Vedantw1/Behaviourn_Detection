package com.behavior.collector.events;

/**
 * Enumeration of mouse event types captured during a recording session.
 */
public enum MouseEventType {
    MOVE,
    LEFT_CLICK,
    RIGHT_CLICK,
    SCROLL
}
